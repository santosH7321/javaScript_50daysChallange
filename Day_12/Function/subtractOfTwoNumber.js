//Write a function that takes two numbers as arguments and returns their subtraction .
// Define a function that takes two parameters
function subNumbers(num1, num2) {
    // Sub the two parameters and store the result in a variable
    const sub = num1 - num2;
    // Return the sub
    return sub;
  }
  
  // Call the function and output the result to the console
  console.log(subNumbers(10, 5)); 